apiVersion: apps/v1
kind: Deployment
metadata:
  name: utn
  labels:
    app: web
spec:
  selector:
    matchLabels:
      app: web
  replicas: 5
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
       —name: frontend
          image: atu
          ports:
           —containerPort: 80
